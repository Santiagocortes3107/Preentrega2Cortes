export const ItemDetail = ({car}) => {
return(<>
    <div>{car.car_factory}</div>
    <div>{car.car_factory}</div>
    <div>{car.car_factory}</div>
    <div>{car.car_factory}</div>
    <img src={car.img} alt ={car.car_model} /></>
)
}